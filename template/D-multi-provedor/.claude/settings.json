{
  "permissions": {
    "allow": [
      "Read(*)",
      "Write(memory/**)",
      "Write(changes/**)",
      "Write(projects/**)",
      "Bash(git status)",
      "Bash(git log*)",
      "Bash(git diff*)",
      "Bash(git branch*)",
      "Bash(git commit*)"
    ],
    "deny": [
      "Bash(rm -rf*)",
      "Bash(git push --force*)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'dir=\"${CLAUDE_PROJECT_DIR:-.}/memory/history\"; mkdir -p \"$dir\"; f=\"$dir/$(date +%Y-%m-%d-%H)-sessao.md\"; printf \"# Sessao %s\\n\\nSessao encerrada automaticamente.\\nExecutar /wrapup para consolidar e finalizar o handoff (memory/handoff.md).\\n\" \"$(date \"+%Y-%m-%d %H:%M\")\" > \"$f\"'"
          }
        ]
      }
    ]
  }
}
