# Ponteiro — cérebro canônico em AGENTS.md

> Este projeto é multi-provedor. O orquestrador real (regras, workers, ciclo, estado)
> vive em `AGENTS.md`, compartilhado por todas as IAs. Este arquivo é só um ponteiro fino
> para o Claude Code — **não duplicar conteúdo aqui** (evita drift / "delírio").

@AGENTS.md
